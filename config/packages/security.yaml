security:
  encoders:
    auto:
      algorithm: auto

  providers:
    users:
      id: 'EvidApp\User\Infrastructure\Auth\AuthProvider'


  firewalls:
    dev:
      pattern: ^/(_(profiler|wdt)|css|images|js)/
      security: false

    auth:
      pattern:  ^/auth/check
      stateless: true
      anonymous: true

    signup:
      pattern:  ^/auth/signup
      stateless: true
      anonymous: true

    secured:
      pattern:  ^/
      provider: users
      stateless: true
      guard:
        authenticators:
          - lexik_jwt_authentication.jwt_token_authenticator

  access_control:
    - { path: ^/auth/check,    roles: IS_AUTHENTICATED_ANONYMOUSLY }
    - { path: ^/auth/signup,  roles: IS_AUTHENTICATED_ANONYMOUSLY }
    - { path: ^/,        roles: IS_AUTHENTICATED_FULLY       }
